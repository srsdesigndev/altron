tailwind.config = {
    darkMode: 'class',
    theme: {
        extend: {
            colors: {
                // Light theme
                'bg-primary': '#f5f5f7',
                'bg-secondary': '#ffffff',
                'bg-tertiary': '#fafafa',
                // Dark themes handled via CSS variables
            }
        }
    }
}